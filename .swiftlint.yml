# SwiftLint configuration for SwiftEntryKit
#
# Formatting (indentation, line wrapping, whitespace, braces, commas) is owned
# by SwiftFormat. SwiftLint is reserved for semantic checks only.
# Run locally with: mise run lint

included:
  - Source
  - Tests
  - Example/SwiftEntryKit

excluded:
  - Example/Derived

analyzer_rules:
  - unused_import
  - unused_declaration

opt_in_rules:
  - array_init
  - contains_over_filter_count
  - contains_over_filter_is_empty
  - contains_over_first_not_nil
  - contains_over_range_nil_comparison
  - empty_collection_literal
  - empty_count
  - empty_string
  - empty_xctest_method
  - extension_access_modifier
  - fallthrough
  - fatal_error_message
  - first_where
  - flatmap_over_map_reduce
  - identical_operands
  - implicit_return
  - joined_default_parameter
  - last_where
  - legacy_multiple
  - legacy_random
  - overridden_super_call
  - pattern_matching_keywords
  - prefer_zero_over_explicit_init
  - private_action
  - private_outlet
  - reduce_into
  - redundant_nil_coalescing
  - redundant_type_annotation
  - toggle_bool
  - trailing_closure
  - unavailable_function
  - unneeded_parentheses_in_closure_argument
  - untyped_error_in_catch
  - yoda_condition

# Formatting rules are delegated to SwiftFormat; size/complexity limits are
# delegated to code review. The library intentionally nests types deeply.
disabled_rules:
  - opening_brace
  - trailing_comma
  - trailing_whitespace
  - trailing_newline
  - trailing_semicolon
  - leading_whitespace
  - vertical_whitespace
  - colon
  - comma
  - control_statement
  - mark
  - return_arrow_whitespace
  - statement_position
  - closure_parameter_position
  - redundant_optional_initialization
  - syntactic_sugar
  - line_length
  - file_length
  - type_body_length
  - function_body_length
  - function_parameter_count
  - nesting
  - multiple_closures_with_trailing_closure
  - computed_accessors_order
  - static_over_final_class

cyclomatic_complexity:
  warning: 20
  error: 30

identifier_name:
  min_length: 3
  max_length: 50
  excluded:
    - id
    - x
    - y
    - z
    - by

type_name:
  min_length: 3
  max_length: 60
